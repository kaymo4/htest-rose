default: &default
  adapter: postgresql
  encoding: utf8
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  <<: *default
  database: mokanova_htest_rose_bk
  port: 5432
  host: localhost
  user: kaymo
test:
  <<: *default
  database: htest-rose-test
  port: 5432
  host: localhost
  user: kaymo